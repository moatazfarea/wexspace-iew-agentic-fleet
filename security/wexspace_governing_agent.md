# WEXSPACE GOVERNING AGENT

Owns the R07 competition context allowlist, authority and scope gates, bounded
delegation, persistent workflow state, and the human-release gate. It cannot
perform the engineering calculation or approve its own result.

